##  Day 2 — HTTP Methods, Path & Query Parameters

###  Goals

- Understand HTTP methods and CRUD operations
- Learn path parameters
- Learn query parameters
- Understand required vs optional parameters
- Use `Query()` for validation
- Use `Path()` for validation
- Understand FastAPI's `422 Unprocessable Entity` validation response
- Combine path and query parameters
- Practice Git workflow

---

